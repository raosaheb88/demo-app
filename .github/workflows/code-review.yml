name: Code Review

on:
  workflow_dispatch:

jobs:
  review:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Code Review
        run: |
          echo "Code Review Started"
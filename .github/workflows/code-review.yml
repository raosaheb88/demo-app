name: Code Review

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  code-review:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Trigger Code Review API
        shell: bash
        run: |
          echo "Starting code review workflow..."

          REPO_URL="${{ github.server_url }}/${{ github.repository }}.git"

          PR_NUMBER="${{ github.event.pull_request.number }}"
          if [ -z "$PR_NUMBER" ]; then
            PR_NUMBER=0
          fi

          SOURCE_BRANCH="${{ github.event.pull_request.head.ref || github.ref_name }}"
          JIRA_ID=$(echo "$SOURCE_BRANCH" | grep -oE 'RD-[0-9]+' | head -1)

          if [ -z "$JIRA_ID" ]; then
            JIRA_ID="NA"
          fi

          cat > request.json <<EOF
          {
            "repoUrl": "$REPO_URL",
            "prNumber": $PR_NUMBER,
            "jiraId": "$JIRA_ID"
          }
          EOF

          echo "========== REQUEST =========="
          cat request.json

          echo "========== TEST EC2 CONNECTION =========="
          curl -v \
            --connect-timeout 10 \
            --max-time 20 \
            http://65.0.72.161:8080/actuator/health

          echo "========== CALL CODE REVIEW API =========="
          curl -v \
            --fail-with-body \
            --connect-timeout 10 \
            --max-time 120 \
            -X POST "http://13.235.113.136:8080/api/review" \
            -H "Content-Type: application/json" \
            -d @request.json \
            -o review-response.json

          echo "========== RESPONSE =========="
          cat review-response.json

      - name: Publish Report To Summary
        if: always()
        shell: bash
        run: |
          echo "# 🤖 AI Code Review Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "## Request" >> $GITHUB_STEP_SUMMARY
          echo '```json' >> $GITHUB_STEP_SUMMARY
          if [ -f request.json ]; then cat request.json >> $GITHUB_STEP_SUMMARY; else echo "request.json not found" >> $GITHUB_STEP_SUMMARY; fi
          echo '```' >> $GITHUB_STEP_SUMMARY

          echo "" >> $GITHUB_STEP_SUMMARY

          echo "## Response" >> $GITHUB_STEP_SUMMARY
          echo '```json' >> $GITHUB_STEP_SUMMARY
          if [ -f review-response.json ]; then cat review-response.json >> $GITHUB_STEP_SUMMARY; else echo "review-response.json not found" >> $GITHUB_STEP_SUMMARY; fi
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Add Review Comment To PR
        if: github.event_name == 'pull_request' && success()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            const request = fs.readFileSync('request.json', 'utf8');
            const response = fs.readFileSync('review-response.json', 'utf8');

            const body =
              "## 🤖 AI Code Review Report\n\n" +
              "### Request Payload\n" +
              "```json\n" +
              request +
              "\n```\n\n" +
              "### Review Response\n" +
              "```json\n" +
              response +
              "\n```\n\n" +
              "✅ Code review completed successfully.";

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: body
            });

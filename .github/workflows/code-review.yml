name: Code Review

on:
  workflow_dispatch:

jobs:
  review:
    runs-on: self-hosted

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Trigger Code Review Application
        run: |
          curl http://localhost:8081/api/review
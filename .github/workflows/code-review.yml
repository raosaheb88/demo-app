name: Code Review

on:
  workflow_dispatch:

jobs:
  review:
    runs-on: self-hosted

    steps:
      - name: Test
        shell: pwsh
        run: |
          Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process -Force
          echo "Hello"